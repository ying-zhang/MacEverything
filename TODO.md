# TODO

## File interactions

- [ ] Add `Command+Enter` to reveal the selected result in Finder while keeping plain `Enter` as the default open action.
- [ ] Add an `Open With...` submenu to result context menus, separate from the default `Open` action.
